#!/bin/bash

touch /usr/local/pyenv/versions/<%= node['exabgp']['python']['version'] %>/etc/exabgp/exabgp.env
mkfifo /run/exabgp.{in,out}
chmod 600 /run/exabgp.{in,out}
chown 99:99 /run/exabgp.{in,out}
